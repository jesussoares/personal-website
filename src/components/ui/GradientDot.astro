---
interface Props {
  class?: string;
  style?: string;
}

const { class: className = 'size-1.5', style } = Astro.props;
---

<span class:list={['rounded-full bg-gradient-to-r from-accent-a to-accent-b', className]} style={style} aria-hidden="true"
></span>
